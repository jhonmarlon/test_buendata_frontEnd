

En el archivo app.js se encontrará una variable llamada baseUrl que nos define la url de nuestra API, es necesario cambiarla solo si se ejecuta en un servidor fuera del local.

Para correr el proyecto es necesario ejecutar el comando `npm start`



